const 
    _ = require('lodash'),
    mapper = require('./general-mapper');

module.exports = {
    mapCucumber: function(cucumberPayload) {
        /*
        var result = {
            subject: getSubject(cucumberPayload),
            assessment: getAssessment(cucumberPayload),
            exam: getExam(cucumberPayload)
        };
        */
        //const subject = mapper.mapResource()
        return undefined;
    }    
};
